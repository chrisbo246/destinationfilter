
# GLOBAL NPM INSTALLATION __________________________________________________________________________

# List available plugins on NPM
npm search grunt

# Install Node package manager NPM
sudo npm install npm -g
npm -v
npm config get prefix

# Install a package
sudo npm install -g jshint

# Update packages
npm install -g jshint

# Unistall a package
npm uninstall -g jshint

# Upgrade node
sudo npm cache clean -f
sudo npm install -g n
sudo n stable



# LOCAL NPM INSTALLATION ___________________________________________________________________________

# Install NPM
cd /project/path/
npm install lodash --save --save-dev

# Update a package
npm update

# Remove a package
npm uninstall --save lodash

# GRUNT ____________________________________________________________________________________________

# Install grunt
npm install -g grunt-cli

# run grunt
cd /my/project/
grunt -f -v --max-stack-size=10000 --debug --verbose build



# BOOTSTRAP Packages _______________________________________________________________________________

# Install dependencies globaly
sudo npm install -g btoa glob grunt grunt-autoprefixer grunt-banner grunt-contrib-clean grunt-contrib-compress grunt-contrib-concat grunt-contrib-connect grunt-contrib-copy grunt-contrib-csslint grunt-contrib-cssmin grunt-contrib-jade grunt-contrib-jshint grunt-contrib-less grunt-contrib-qunit grunt-contrib-uglify grunt-contrib-watch grunt-csscomb grunt-exec grunt-html-validation grunt-jekyll grunt-jscs grunt-saucelabs grunt-sed load-grunt-tasks npm-shrinkwrap remarkable time-grunt



# __________________________________________________________________________________________________

npm install -g i18next-conv --save-dev

# Convert gettext to i18next JSON
i18next-conv -l en -s ./locales/en.po -t ./locales/en/translation.json

Convert i18next JSON to gettext files
i18next-conv -l en -s ./locales/en/alerts.json -t ./locales/en/alerts.po
i18next-conv -l en -s ./locales/en/buttons.json -t ./locales/en/buttons.po
i18next-conv -l en -s ./locales/en/fields.json -t ./locales/en/fields.po
i18next-conv -l en -s ./locales/en/global.json -t ./locales/en/global.po
i18next-conv -l en -s ./locales/en/priorities.json -t ./locales/en/priorities.po
i18next-conv -l en -s ./locales/en/questions.json -t ./locales/en/questions.po
i18next-conv -l en -s ./locales/en/sections.json -t ./locales/en/sections.po
i18next-conv -l en -s ./locales/en/values.json -t ./locales/en/values.po



# New project ______________________________________________________________________________________

cd /my/project
# Create and edit Gruntfile.js
# Create and edit package.json
# (Remove node_modules directory if exists)
npm cache clean
npm install

# My plugins _______________________________________________________________________________________

npm install -g --save-dev grunt-contrib-htmlmin grunt-revizor grunt-contrib-imagemin grunt-remove-logging grunt-json-minify


# Best grunt plugins _______________________________________________________________________________

# Check code style
npm install -g --save-dev grunt-jscs grunt-complexity

# Prettify
npm install -g --save-dev grunt-prettify

# Combine
npm install -g --save-dev grunt-html-build grunt-contrib-concat

# Compile
npm install -g --save-dev grunt-contrib-less grunt-contrib-handlebars

# Browser capabilities
# npm install -g --save-dev autoprefixer grunt-autopolyfiller grunt-modernizr

# Minify
npm install -g --save-dev grunt-contrib-htmlmin grunt-revizor grunt-contrib-uglify grunt-contrib-imagemin grunt-json-minify

# Validate
npm install -g --save-dev grunt-htmllint # grunt-csscomb grunt-contrib-jshint fixmyjs grunt-jsonlint grunt-yamllint

# Remove unneeded
npm install -g --save-dev grunt-uncss grunt-ucss grunt-remove-logging

# Performance test
npm install -g --save-dev grunt-phantomas psi tmi

grunt-convert
grunt-browser-sync
grunt-email-design
grunt-responsive-images grunt-sharp
grunt-release and grunt-version

grunt-contrib-compress
grunt-contrib-clean and grunt-contrib-copy
grunt-usebanner
grunt-json-bake
grunt-jsonlint
json-structure-comparator

# grunt-contrib-csslint
grunt-jsfmt 


uglify-save-license
jshint-stylish-ex
grunt-lib-phantomjs
gruntfile-editor
grunt-contrib-requirejs

sudo npm update -g --save-dev btoa glob grunt grunt-autoprefixer grunt-banner grunt-contrib-clean grunt-contrib-compress grunt-contrib-concat grunt-contrib-connect grunt-contrib-copy grunt-contrib-csslint grunt-contrib-cssmin grunt-contrib-htmlmin grunt-contrib-imagemin grunt-contrib-jshint grunt-contrib-less grunt-contrib-nodeunit grunt-contrib-qunit grunt-contrib-uglify grunt-contrib-watch grunt-csscomb grunt-exec grunt-htmllint grunt-jekyll grunt-jscs grunt-json-minify grunt-jsonmin grunt-minjson grunt-prettify grunt-processhtml grunt-remove-logging grunt-revizor grunt-saucelabs grunt-sed grunt-uncss http jquery json-structure-comparator load-grunt-tasks remarkable time-grunt

# Bower ____________________________________________________________________________________________

# Removed (ol3)
d3 proj4 topojson